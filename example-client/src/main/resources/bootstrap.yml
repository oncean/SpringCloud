server:
  port: 9002

spring:
  application:
    name: example-client
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8851
      config:
        server-addr: 127.0.0.1:8851
        file-extension: yaml
    sentinel:
      transport:
        port: 9002
        dashboard: localhost:8858
